diff --git a/build.zig b/build.zig
index 37523e6..b1c2d3f 100644
--- a/build.zig
+++ b/build.zig
@@ -742,6 +742,10 @@ fn addCompilerStep(b: *std.Build, options: AddCompilerModOptions) *std.Build.Step
     });
     exe.stack_size = stack_size;

+    // Allow undefined symbols in shared library dependencies (libc++.so, libc++abi.so
+    // from zig-llvm may reference glibc symbols newer than the target version).
+    exe.linker_allow_shlib_undefined = true;
+
     return exe;
 }
